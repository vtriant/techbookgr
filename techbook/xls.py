import xlrd
from os.path import join, dirname, abspath
from rest_framework.response import Response
from django.shortcuts import render, redirect
'''
def runme(request):
    #fname = join(dirname(dirname(abspath(__file__))), 'test_data', '3.xls')
    #workbook = xlrd.open_workbook('http://webcraft.gr/3.xls')
    #worksheet = workbook.sheet_by_name('3')
    #worksheet = workbook.sheet_by_index(0)
    #le = worksheet.cell(0, 0).value
    #return Response("BAM!")
'''